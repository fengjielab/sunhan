# Pilot data only

Store pilot CSV, events JSON, summary JSON, run configuration, outcome records, and
validation reports here. Never copy pilot trials into `06_formal_data` or include them
in confirmatory inference.
