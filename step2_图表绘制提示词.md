# Step 2：图表绘制提示词

> 每个图给出**完整提示词**，您复制到对应工具中使用。
> 工具推荐：Python (matplotlib)、draw.io、Illustrator、Visio

---

## 图1：现有方法 vs. 本文方法对比示意图

**推荐工具：** draw.io / Illustrator / Visio

**提示词（给绘图AI或美工）：**

```
请绘制一张双栏宽的彩色对比框图，左右分栏对比"传统方法"和"本文方法"。

【左侧 — 传统方法】
三个独立方框纵向排列，用灰色箭头连接：

方框1（顶部，绿色）："视觉模块" → 仅输出"目标位置"
方框2（中部，红色）："力反馈" → 标注"K_trans = 固定值"
方框3（底部，蓝色）："阻抗控制" → 标注"K = 200 N/m 固定"

三个方框之间互相独立，没有交叉连接。

【右侧 — 本文方法】
一个中央视觉模块分叉驱动两个模块：

方框1（顶部，绿色）："YOLOv11n 视觉语义" → 识别出"物体类别 c"
从方框1分出两个绿色箭头：
  箭头A → 指向左侧红色方框"力反馈调度器" → 标注"K_trans(c) 自适应"
  箭头B → 指向右侧蓝色方框"阻抗调度器" → 标注"K(c), D(c) 自适应"

【颜色规范】
- 蓝色（#2196F3）：运动控制流
- 红色（#F44336）：力反馈流  
- 绿色（#4CAF50）：视觉语义流
- 背景：白色
- 文字：黑色，中文

【标注】
左侧底部标注"传统方法：参数固定，各模块独立"
右侧底部标注"本文方法：视觉语义驱动双调度"
```

---

## 图2：系统总体架构图

**推荐工具：** draw.io / Illustrator / Visio

**提示词（给绘图AI或美工）：**

```
请绘制一张上下分层的彩色系统架构图，包含三层结构。

【顶层 — 主端（Omega.7）】
三个方框水平排列：
  方框A："位姿读取 (200Hz)" → 箭头向下
  方框B："力反馈渲染 (200Hz)" ← 箭头从下方接入
  右上角标注"主端 (Omega.7)"

【中层 — 共享控制器（主控机）】
四个方框用箭头连接：
  方框A："位置映射 (scale×sign)"
  方框B："自适应导纳调度 K(c), D(c)"
  方框C："力反馈调度器 K_trans(c) + deadband"
  方框D："夹持力估计器 f_grip = ||τ_wrist||"

  方框A → 方框B → 蓝色箭头向下（标注"目标位姿 Xd"）
  方框C ← 红色箭头从下方接入
  方框D ← 红色箭头从下方接入

【左侧 — 视觉模块（独立进程 30Hz）】
  左侧独立区块：
    "RealSense D435" → "YOLOv11n" → "语义-物理映射表"
  输出绿色箭头分叉：
    一支指向"力反馈调度器"
    一支指向"自适应导纳调度"

【底层 — 从端（Franka Panda）】
  方框A："笛卡尔阻抗控制 (200Hz)" ← 蓝色箭头接入
  方框B："腕部力矩传感器 tau_ext" → 红色箭头向上
  
  右下角标注"从端 (Franka Panda + Franka Hand)"

【颜色规范】
- 🔵 蓝色 (#2196F3)：运动流路径，线宽2px
- 🔴 红色 (#E53935)：力觉流路径，线宽2px
- 🟢 绿色 (#43A047)：视觉语义流路径，线宽2px
- 所有箭头末端标注频率值（200Hz / 30Hz）
- 中文标注
```

---

## 图3：夹持过程信号曲线

**推荐工具：** Python (matplotlib)

**数据来源：** Step 3b 采集的 CSV 数据（`step_3b_grip_test.py` 输出）

**提示词：**

```
使用 Python matplotlib 绘制双子图纵向排列。

【上子图 — 夹爪宽度】
- 横轴：时间 (s)，范围 0~8s
- 纵轴：夹爪宽度 (mm)，范围 0~80mm
- 曲线：蓝色实线，线宽1.5
- 数据来源：CSV中的 width 列 × 1000（转换为mm）

【下子图 — 腕部力矩范数】
- 横轴：时间 (s)，范围 0~8s（与上子图对齐）
- 纵轴：腕部力矩范数 (N·m)，范围 0~5 N·m
- 曲线：红色实线，线宽1.5
- 数据来源：CSV中的 tau_wrist_norm 列

【关键标注（两张子图都需要）】
- 在夹爪宽度"停滞"位置画一条垂直虚线，标注"接触点"
- 在腕部力矩"上升沿"对应位置画同一条垂直虚线
- 在力矩曲线上加一条水平灰色虚线表示阈值 (1.0 N·m)
- 用浅色背景区分三个阶段：
  阶段1（0s~接触点前）：标注"接近阶段"
  阶段2（接触点~释放前）：标注"夹持阶段"
  阶段3（释放后）：标注"释放阶段"

【格式】
- 双图共享横轴标签"时间 (s)"
- 上子图纵轴标签"夹爪宽度 (mm)"
- 下子图纵轴标签"腕部力矩范数 (N·m)"
- 字号：10pt，字体：SimHei（中文）
- 图尺寸：单栏宽 (70mm × 80mm)
- 输出：fig3_grip_signal.png, DPI=300
- 网格：浅灰色虚线网格
```

---

## 图4：阻抗响应对比曲线 ⭐ 可先行绘制

**推荐工具：** Python (matplotlib) / Matlab

**提示词：**

```
使用 Python matplotlib + scipy 仿真生成二阶系统阶跃响应曲线。

【系统参数】
- 质量：M = 3.0 kg
- 激励：阶跃外力 F = 2 N（在 t=0.1s 时施加）
- 阻尼：临界阻尼 D = 2*sqrt(M*K)

【三条曲线】
| 曲线 | 刚度 K | 阻尼 D | 颜色 | 线型 |
|------|--------|--------|------|------|
| 软物体 | 50 N/m | 24.5 N·s/m | 绿色 (#4CAF50) | 实线 |
| 中刚度 | 150 N/m | 42.4 N·s/m | 橙色 (#FF9800) | 实线 |
| 硬物体 | 300 N/m | 60.0 N·s/m | 红色 (#F44336) | 实线 |

【代码骨架】
```python
import numpy as np
import matplotlib.pyplot as plt
from scipy.integrate import odeint

M = 3.0
configs = [
    (50, 2*np.sqrt(3*50), '#4CAF50', 'K=50 N/m (软)'),
    (150, 2*np.sqrt(3*150), '#FF9800', 'K=150 N/m (中)'),
    (300, 2*np.sqrt(3*300), '#F44336', 'K=300 N/m (硬)'),
]

def system(y, t, K, D):
    x, v = y
    F = 2.0 if t >= 0.1 else 0.0
    return [v, (F - K*x - D*v) / M]

t = np.linspace(0, 2, 1000)
plt.figure(figsize=(6, 4))

for K, D, color, label in configs:
    sol = odeint(system, [0, 0], t, args=(K, D))
    plt.plot(t, sol[:, 0], color=color, label=label, linewidth=2)
    plt.axhline(y=2/K, color=color, linestyle='--', alpha=0.4, linewidth=1)

plt.xlabel('时间 (s)', fontsize=11)
plt.ylabel('位移 (m)', fontsize=11)
plt.legend(fontsize=10, loc='lower right')
plt.grid(alpha=0.3)
plt.title('阶跃外力 F=2N 下的阻抗响应', fontsize=12)
plt.xlim(0, 2)
plt.ylim(0, 0.05)
plt.tight_layout()
plt.savefig('fig4_impedance_response.png', dpi=300)
```

【标注要求】
- 三条水平虚线对应各自的稳态偏差值
- 在虚线右侧标注稳态值文字（如 "4.0cm"、"1.3cm"、"0.67cm"）
- 图例在右下角
- 输出 DPI=300
```

---

## 图5：实验平台实物照片

**推荐工具：** 相机拍摄 + 手机/电脑标注

**提示词：**

```
【拍摄设置】
- 设备：手机或相机
- 角度：侧前方约45度，能同时看到左手的Omega.7和右前方的Franka Panda
- 光线：室内正常照明，避免背光
- 桌面：整洁干净，无杂物
- 背景：简洁（白墙或窗帘）

【画面内容（必须包含）】
1. Franka Panda 机械臂（居中偏右，末端装夹爪）
2. Omega.7 力反馈主手（左下角，操作员坐的位置）
3. RealSense D435 深度相机（固定在机械臂上方或前方支架上）
4. 被抓物体（如苹果/香蕉/水瓶，放在机械臂前方桌面）
5. 主控机/工控机（可在背景中）

【标注（用红色箭头+黑色文字）】
① Omega.7 主手  → 指向主手设备
② Franka Panda 机械臂  → 指向机械臂本体
③ RealSense D435  → 指向深度相机
④ 目标物体  → 指向被抓物体
⑤ 主控机 (i7-12700)  → 指向工控机

【后期处理】
- 适当调亮，对比度微调，确保设备轮廓清晰
- 标注文字：黑体、中文、字号统一
- 箭头：红色、线宽适中、不遮挡设备
- 输出：fig5_experiment_setup.jpg / 约140mm × 90mm
```

---

## 图6：NASA-TLX 评分柱状图

**推荐工具：** Python (matplotlib) / Excel

**数据来源：** 实验后统计的 NASA-TLX 问卷数据

**提示词：**

```
使用 Python matplotlib 绘制分组柱状图。

【数据格式（示例，替换为实际数据）】
dimensions = ['脑力需求', '体力需求', '时间需求', '努力程度', '任务表现', '挫败感']
mode_a_scores = [12, 10, 11, 13, 14, 10]  # 替换为实际均值
mode_b_scores = [9, 8, 8, 10, 10, 7]     # 替换为实际均值
mode_c_scores = [5, 4, 5, 5, 6, 3]       # 替换为实际均值

mode_a_std = [2, 1.5, 2, 2.5, 2, 1.5]    # 标准差
mode_b_std = [1.5, 1, 1.5, 2, 1.5, 1]
mode_c_std = [1, 0.5, 1, 1, 1, 0.5]

【绘图参数】
- 柱状图类型：分组柱状图（每组3根柱子并列）
- 柱子颜色：模式A → 红色 (#E53935)，模式B → 黄色 (#FFC107)，模式C → 绿色 (#43A047)
- 柱子宽度：0.25
- 柱子顶部标注数值（保留1位小数）
- 误差棒显示标准差（黑色短线，帽宽0.1）

【格式】
- 横轴：6个维度名称（中文）
- 纵轴：评分 (0-20分)，刻度从0到20，步长5
- 图例：右上角，标注"模式A 传统"、"模式B 固定增益"、"模式C 本文方法"
- 标题："三种模式的 NASA-TLX 评分对比"
- 字体：SimHei（中文），字号10pt
- 尺寸：双栏宽 (140mm × 70mm)
- 网格：仅Y轴方向浅灰色虚线
- 输出：fig6_nasa_tlx.png, DPI=300
```

---

## 图7：末端轨迹对比图

**推荐工具：** Python (matplotlib)

**数据来源：** 实验CSV中的末端位置数据（`panda_position_x/y/z` 列）

**提示词：**

```
使用 Python matplotlib 绘制左右双子图的末端轨迹对比。

【数据准备】
# 从 mode_a_apple.csv 和 mode_c_apple.csv 中提取
# 选取桌面平面坐标 (x, y)，单位 mm
traj_a_x = [...]  # 模式A的x坐标 * 1000 (转为mm)
traj_a_y = [...]  # 模式A的y坐标 * 1000 (转为mm)
traj_c_x = [...]  # 模式C的x坐标 * 1000 (转为mm)
traj_c_y = [...]  # 模式C的y坐标 * 1000 (转为mm)

【左子图 — 模式A（传统方法）】
- 轨迹颜色：红色 (#E53935)
- 线型：实线，线宽1.5
- 起点：绿色圆点 (marker='o', s=50)，标注"起点"
- 终点：红色星号 (marker='*', s=100)，标注"目标"
- 特征：路径应长而弯曲，有多处回退/震荡

【右子图 — 模式C（本文方法）】
- 轨迹颜色：绿色 (#43A047)
- 线型：实线，线宽1.5
- 起点：绿色圆点 (marker='o', s=50)，标注"起点"
- 终点：红色星号 (marker='*', s=100)，标注"目标"
- 特征：路径应短而直，平滑直达

【格式】
- 左上角标注子图编号 (a) 和 (b)
- 横轴：X (mm)
- 纵轴：Y (mm)
- 两子图坐标范围保持一致（方便对比）
- 网格：浅灰色虚线
- 字体：10pt
- 尺寸：双栏宽 (140mm × 60mm)
- 输出：fig7_trajectory.png, DPI=300
```

---

## 图8：力反馈曲线对比

**推荐工具：** Python (matplotlib)

**数据来源：** 实验CSV中的 `haptic_force_z` 列

**提示词：**

```
使用 Python matplotlib 绘制上下双子图的力反馈曲线对比。

【数据来源】
# 从 mode_c_apple.csv 和 mode_c_book.csv 中提取 haptic_force_z
time_apple = [...]     # 抓取苹果时的时间 (s)
fz_apple = [...]       # 抓取苹果时的 Z方向力反馈 (N)
time_book = [...]      # 抓取书籍时的时间 (s)
fz_book = [...]        # 抓取书籍时的 Z方向力反馈 (N)

【上子图 — 抓取苹果】
- 曲线颜色：绿色 (#4CAF50)，线宽1.5
- Y轴范围：-1 ~ 5 N（或根据数据调整）
- 特征：力反馈幅值低 (<1N)，波动小

【下子图 — 抓取书籍】
- 曲线颜色：红色 (#F44336)，线宽1.5
- Y轴范围：-1 ~ 5 N（与上子图一致）
- 特征：力反馈幅值高 (>3N)，波动明显

【关键标注（两张子图都需要）】
用垂直虚线将曲线分为三个阶段：
阶段1 — "接触建立"：力从零开始上升的位置
阶段2 — "稳定夹持"：力保持稳定的区间
阶段3 — "释放"：力归零的位置

加一条水平灰色虚线表示死区范围：
- 苹果子图：deadband = 0.3 N
- 书籍子图：deadband = 0.5 N

在曲线峰值处标注力值（如 "F_peak = 0.8N"、"F_peak = 3.5N"）

【格式】
- 双图共享横轴标签"时间 (s)"
- 纵轴标签"力反馈 F_z (N)"
- 左上方标注"(a) 抓取苹果"和"(b) 抓取书籍"
- 字体：10pt
- 尺寸：单栏宽 (70mm × 80mm)
- 网格：浅灰色虚线
- 输出：fig8_force_feedback.png, DPI=300
```

---

## 表1：语义-物理属性映射表

**推荐工具：** Word / LaTeX / Markdown

**提示词：**

```
用三线表格式绘制：

表1：物体类别与物理控制参数映射表

| 物体类别 c | 阻抗刚度 K(N/m) | 阻尼 D(N·s/m) | 目标夹持力 F_target(N) | 力反馈增益 K_trans | 死区 deadband(N) |
|------------|----------------|---------------|----------------------|-------------------|-----------------|
| 苹果(软)   | 50             | 24.5          | 8.0                  | 0.3               | 0.3             |
| 香蕉(软)   | 50             | 24.5          | 6.0                  | 0.3               | 0.3             |
| 塑料瓶(中) | 150            | 42.4          | 15.0                 | 0.5               | 0.4             |
| 书籍(硬)   | 300            | 60.0          | 25.0                 | 1.0               | 0.5             |
| 手机(硬)   | 300            | 60.0          | 20.0                 | 1.0               | 0.5             |

注：① 阻尼按临界阻尼条件设计，等效质量 M_eff = 3.0 kg
    ② 阻抗刚度 K 和阻尼 D 为笛卡尔平移方向参数（旋转方向固定为 10 N·m/rad）
    ③ Z 轴刚度可根据物体标签打折（soft×0.5, medium×0.8, hard×1.0）

格式要求：
- 三线表格式（顶线、表头下线、底线）
- 表头加粗，数值居中对齐
- 表注放在表格下方，字号比正文小一号
```

---

## 表2 & 表3：实验数据表

**推荐工具：** 等实验完成后，运行 `plans/experiment_analysis.py` 自动生成

```bash
python3 plans/experiment_analysis.py data/experiment_*/operator_*/ --output ./paper_figures
```

该脚本会自动生成表2（成功率与任务时间）、表3（软物体破损率）、以及对应的LaTeX代码和图表。

如果不方便运行Python脚本，手动填写以下模板：

**表2：成功率与任务时间对比**
| 模式 | 苹果 | 香蕉 | 塑料瓶 | 书籍 | 手机 | 平均成功率 | 平均时间(s) |
|------|------|------|--------|------|------|-----------|------------|
| 模式A | / | / | / | / | / | / | / |
| 模式B | / | / | / | / | / | / | / |
| 模式C | / | / | / | / | / | / | / |

**表3：软物体破损率对比**
| 模式 | 苹果破损率 | 香蕉破损率 | 平均破损率 |
|------|-----------|-----------|-----------|
| 模式A | / | / | / |
| 模式B | / | / | / |
| 模式C | / | / | / |

---

## 快速参考卡

| 图号 | 工具 | 数据依赖 | 建议先做？ |
|------|------|---------|-----------|
| 图1 | draw.io / AI绘图 | ❌ 无 | ⭐ 可并行 |
| 图2 | draw.io / AI绘图 | ❌ 无 | ⭐ 可并行 |
| 图3 | Python matplotlib | ✅ Step 3b 数据 | 等实验 |
| 图4 | Python matplotlib | ❌ 无，可仿真 | ⭐⭐⭐ **最优先** |
| 图5 | 相机拍摄 | ✅ 需现场拍照 | 等实验 |
| 图6 | Python matplotlib | ✅ NASA-TLX数据 | 等实验 |
| 图7 | Python matplotlib | ✅ 实验CSV数据 | 等实验 |
| 图8 | Python matplotlib | ✅ 实验CSV数据 | 等实验 |
